#!/bin/sh

case `hig format` in
git )
  CMD="git pull"
  ;;
svn )
  CMD="svn update"
  ;;
hg )
  CMD="hg pull -u"
  ;;
* )
  fatal "pull: unknown command for repo type: `hig format`"
  ;;
esac

exec $CMD "$@"
