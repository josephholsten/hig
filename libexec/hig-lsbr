#!/bin/sh
# hig-lsbr: list branches

. $_HIG_ROOT/lib/common.sh

fmt=`hig format`
case $fmt in
git )
  CMD="git branch"
  ;;
hg )
  CMD="hg branches"
  ;;
* )
  fatal "lsbr: unknown command for repo type: $fmt"
  ;;
esac

exec $CMD "$@"
